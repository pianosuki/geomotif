[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "geomotif"
dynamic = ["version"]
description = "Generate and plot geometric designs, with true arc-length point placement and pluggable spacing curves"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [
    { name = "Scott Weer", email = "pianosuki@protonmail.com" },
]
keywords = [
    "geometry",
    "motif",
    "pattern",
    "points",
    "easing",
    "arc-length",
    "generative-art",
    "creative-coding",
    "plotter",
    "pen-plotter",
    "spiral",
    "design",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Other Audience",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Artistic Software",
    "Topic :: Multimedia :: Graphics",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
plot = ["matplotlib>=3.10"]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-cov>=6",
    "matplotlib>=3.10",
    "ruff>=0.16",
    "mypy>=1.14",
]

[project.urls]
# TODO: update these once the repository is published
Homepage = "https://github.com/pianosuki/geomotif"
Issues = "https://github.com/pianosuki/geomotif/issues"
Changelog = "https://github.com/pianosuki/geomotif/blob/main/CHANGELOG.md"

[project.scripts]
# Becomes `geomotif = "geomotif.cli:main"` once the real CLI lands.
geomotif-demo = "geomotif.demo:main"

[tool.hatch.version]
path = "src/geomotif/__init__.py"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
# Pinned to the supported floor, not the newest interpreter, so a
# 3.13-or-later idiom cannot slip in unnoticed.
target-version = "py312"
line-length = 100
src = ["src", "tests", "examples"]
# ruff formats Python blocks inside Markdown; the refactor plan's snippets are
# illustrative pseudocode (bare `...` bodies, signatures without implementations)
# and are meant to read as prose, not to survive a formatter.
extend-exclude = ["REFACTOR_PLAN.md"]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort
    "UP",     # pyupgrade
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "SIM",    # flake8-simplify
    "N",      # pep8-naming
    "RUF",    # ruff-specific rules
    "PTH",    # prefer pathlib over os.path
    "PT",     # flake8-pytest-style
    "PIE",    # misc lint rules
    "PERF",   # perflint
    "TC",     # flake8-type-checking
    "RET",    # flake8-return
]
ignore = [
    "E501",   # line length is enforced by the formatter, not worth duplicating
]

[tool.ruff.lint.per-file-ignores]
# PT011: pinning every ValueError to a match= string is brittle busywork
# for input-validation tests where the type alone is the contract.
"tests/*" = ["PT011"]

[tool.ruff.lint.isort]
known-first-party = ["geomotif"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"

[tool.mypy]
python_version = "3.12"
files = ["src", "tests"]
strict = true
warn_unreachable = true
enable_error_code = ["ignore-without-code", "redundant-expr", "truthy-bool"]

[[tool.mypy.overrides]]
module = ["matplotlib.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# Bodies are still checked (check_untyped_defs, inherited from strict) but
# tests aren't required to annotate every `def test_...(): ...`.
module = ["tests.*"]
disallow_untyped_defs = false
disallow_incomplete_defs = false
disallow_untyped_calls = false
